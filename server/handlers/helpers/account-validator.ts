import { Request, Response } from 'express';

export const isInternalUser = async (req: Request, res: Response) => { }

export const isInternalOrParentUser = async (req: Request, res: Response) => { }

Use the text and attached resume format and generate me two pdf copies of my resume. 

One with JetBrain' Mono font-family

another with font-family of your choice. 

Don't use any content from the attached image. 